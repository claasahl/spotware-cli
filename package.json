{
  "name": "spotware-cli",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "preimport": "rimraf ./protobuf",
    "import": "isogit clone --url=https://github.com/spotware/Open-API-2.0-protobuf-messages.git --dir=./protobuf --singleBranch",
    "postimport": "rimraf ./protobuf/.git",
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "pretty-quick --staged",
    "prepare": "npm run build",
    "build": "npm run import && tsc",
    "start": "ts-node src/compile.ts && npx prettier --write ./src/**"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/claasahl/spotware-cli.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/claasahl/spotware-cli/issues"
  },
  "homepage": "https://github.com/claasahl/spotware-cli#readme",
  "devDependencies": {
    "@types/pbf": "^3.0.1",
    "@types/protocol-buffers-schema": "^3.3.0",
    "create-typescript-project-dependencies": "^10.0.4",
    "isomorphic-git": "^0.55.4",
    "rimraf": "^2.6.3"
  },
  "dependencies": {
    "pbf": "^3.2.0",
    "resolve-protobuf-schema": "^2.1.0"
  }
}
